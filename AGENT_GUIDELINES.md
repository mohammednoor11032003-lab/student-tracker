# AGENT_GUIDELINES.md: دستور التطوير وقواعد الجودة الدائمة لمشروع متابعة الطلاب (Student Tracker)

هذا الملف يمثل المرجع الإرشادي والأمني والتقني الأعلى للوكيل الذكي (Antigravity Agent) وفريق التطوير. يجب الالتزام بكل قاعدة واردة فيه في كل مهمة برمجية تُنفَّذ من الآن فصاعداً دون استثناء، ودون الحاجة لتذكير متكرر.

إذا اقتضت الضرورة القصوى مخالفة أو تكييف أي قاعدة لأي ظرف تقني طارئ، **يجب التوقف فوراً وإخبار المستخدم صراحة وطلب الإذن المسبق قبل المتابعة**.

---

## أولاً - قواعد السلوك واتخاذ القرار (Behavioral & Architectural Rules)

### 1. ممنوع الحلول السريعة على حساب الجودة (Root Cause Over Quick Patches)
- يجب معالجة السبب الجذري للمشكلة دائماً، وليس تطبيق أقرب حل مؤقت يُسكت الخطأ أو يُخفيه ظاهرياً.
- تتبّع أصل المشكلة في تدفق البيانات ودورة حياة المكونات وقواعد البيانات قبل كتابة أي سطر علاجي.

### 2. ممنوع اختراع مصادر بيانات بديلة بصمت (No Silent Fallback Inventing)
- إذا فشلت عمليات القراءة أو الكتابة من/إلى قاعدة بيانات Supabase لأي سبب كان، **توقف فوراً وأخبر المستخدم صراحة بالمشكلة**.
- يُحظر تماماً اللجوء التلقائي أو الصامت لبدائل تخزينية مؤقتة (مثل إنشاء ملفات JSON سيرفرية، أو تحويل التخزين إلى `localStorage`/`sessionStorage` للبيانات الدائمة).

### 3. الشفافية الإجبارية (Mandatory Transparency)
- أي حل مؤقت أو بديل استثنائي يُقترح يجب ذكره صراحة للمستخدم مع توضيح مخاطره الفنية والتشغيلية، ولا يجوز إخفاؤه أو تمريره ضمنياً أبداً.

### 4. التحقق الفعلي قبل إعلان النجاح (Proof Before Proclamation)
- يُمنع منعاً باتاً إعلان إنجاز أو نجاح أي مهمة تمس بيانات حقيقية دون تحقق فعلي ملموس.
- يجب تقديم أدلة قطعية: نتائج استعلامات تأكيدية من قاعدة البيانات الحية، واجتياز الفحص البرمجي (`tsc --noEmit` أو اختبارات البناء `npm run build`)، ومقارنة القيم بالأرقام الحقيقية.

### 5. التوقف عند القرارات المعمارية (Stop for Architectural Decisions)
- أي قرار مصيري أو معماري يجب طرحه على المستخدم أولاً ومناقشته قبل البدء في التنفيذ:
  - أين تُخزن البيانات الجديدة (جداول جديدة، حقول، أم كائنات).
  - أي تعديل في بنية الجداول (Schema changes, DDL, Constraints, Foreign Keys).
  - أي حذف أو إعادة هيكلة كبرى لكود قديم قائم.

### 6. الشك بالنجاح السريع وتغطية الحالات الحدّية (Skepticism of Fast Fixes & Edge Cases)
- إذا حُلّت مشكلة معقدة (تتعلق بالتوقيت الزمني، الحسابات المحاسبية/النقاط، التزامن بين أكثر من نظام، أو تعارض الميزات) خلال وقت قصير جداً:
  - توقف وأعد فحص الحل بعمق وشك مهني.
  - تأكد: هل تم تغطية كل السيناريوهات والحالات الحدّية/النادرة (Edge Cases, Null values, Timezones, Race conditions)؟ أم أن الحل يعالج فقط الحالة المحددة المذكورة؟
  - اذكر للمستخدم صراحة أي حالات مشابهة أو سيناريوهات لم يتم اختبارها أو تغطيتها بعد.

### 7. الفحص الدوري لأمان التخزين (Periodic Storage Audits)
- كل أسبوعين تقريباً (أو عند طلب المستخدم)، يجب إجراء مسح شامل للمشروع بحثاً عن أي بيانات تُخزن بطرق غير دائمة أو غير آمنة (ملفات JSON، تخزين محلي غير موثق، انحراف عن جداول Supabase الأساسية).

---

## ثانياً - قواعد الجودة التقنية الصارمة (Strict Technical Standards)

### 1. منع تجاوز نظام الأنواع الصارم (Zero Type Suppression)
- يُمنع استخدام `any` أو التعليقات التجاوزية مثل `@ts-ignore` أو `@ts-nocheck`.
- يجب تعريف كل Interface و Type بدقة واكتمال داخل ملفات الأنواع المخصصة.
- إذا ظهر تعارض حقيقي بين الأنواع، يجب شرح المشكلة التقنية للمستخدم بدلاً من الالتفاف عليها أو إسكات المترجم.

### 2. الأمان المطلق للمفاتيح السرية (Service Role Key Security)
- يُمنع منعاً باتاً استيراد أو استخدام `SUPABASE_SERVICE_ROLE_KEY` داخل أي ملف يحتوي على `'use client'` أو أي كود يُرسل إلى متصفح العميل (Client Bundles).
- الاستخدام المسموح للمفتاح الإداري محصور حصرياً داخل:
  - مسارات الخادم الخلفية (Next.js Route Handlers / API Routes في `src/app/api/...`).
  - مكونات الخادم الآمنة (Server Components) أو دوال الخادم الموثوقة (`*-server.ts`).

### 3. الاستعداد المسبق للنمو والتوسع (Pagination & Limits by Default)
- أي استعلام لجلب قوائم يُتوقع نموها وتراكمها مع الوقت (قوائم الطلاب، سجلات المهام اليومية، الحركات المالية للبنك، سجلات معارك الساحة):
  - يجب أن يتضمن حداً أقصى (`limit`) ودعماً للترقيم/الصفحات (`pagination` / `range`) منذ اليوم الأول، لمنع انهيار الذاكرة أو بطء الاستجابة مع تضخم البيانات.

### 4. عدم ابتلاع الأخطاء بصمت (No Silent Catching)
- يُمنع ترك كتل `catch` فارغة أو الاكتفاء بكتابة `console.log` / `console.error` دون معالجة.
- أي خطأ يؤثر على تجربة المستخدم يجب أن يُترجم إلى استجابة واضحة في الواجهة (إشعار Toast واضح أو Alert باللغة العربية) يشرح باحترام ماذا حدث وما هو الإجراء المطلوب.

### 5. تقسيم المكونات والمسؤولية الواحدة (Component Modularity & Single Responsibility)
- يجب ألا يتجاوز أي ملف React أو مكون واجهة سقف الـ **300 سطر برمجي** تقريباً.
- عند اقتراب أي مكون من هذا الحجم، يجب تفكيكه وإعادة هيكلته إلى مكونات فرعية أصغر (Subcomponents / Hooks / Helpers) يختص كل منها بمهمة واحدة محددة وواضحة.

### 6. تفضيل الخادم على المتصفح (Server-First Heavy Computations)
- العمليات الحسابية الثقيلة (عمليات الفرز المعقدة، التجميع الإحصائي، الحسابات التراكمية، ومطابقة الأرصدة المالية):
  - يجب أن تُنفذ على الخادم (Server Components / Route Handlers) ولا تُلقى على عاتق متصفح الطالب أو العميل.
  - الهدف: ضمان أقصى سرعة واستجابة حتى على الأجهزة الضعيفة وشبكات الإنترنت البطيئة.

---

**تعهد الوكيل (Agent Commitment):**
أقرّ بالالتزام التام والواعي بهذه القواعد كدستور دائم للعمل في هذا المشروع، وسأقوم بمراجعة كل خطوة وتعديل برمجي بناءً عليها قبل وأثناء وبعد التنفيذ.
